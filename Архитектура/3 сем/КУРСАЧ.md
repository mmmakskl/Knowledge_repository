![[asm1.png]]
```mermaid
flowchart TD
    Start["Начало программы"] --> InitData["Инициализация сегментов данных"]
    InitData --> Input["Считывание строки в массив buf"]
    Input --> NewLine["Перенос каретки консоли на новую строку"]
    NewLine --> CountChars["Подсчет количества каждого символа"]
    CountChars --> CheckEndCount["Проверка достижения последнего символа"]
    CheckEndCount -->|Да| StartDiagram["Начало построения диаграммы"]
    CheckEndCount -->|Нет| CountChars
    StartDiagram --> OutputLine["Построение и вывод одной строки диаграммы"]
    OutputLine --> ResetOutput["Возврат массива вывода в начальное состояние"]
    ResetOutput --> CheckDiagramEnd["Проверка завершения диаграммы"]
    CheckDiagramEnd -->|Нет| OutputLine
    CheckDiagramEnd -->|Да| End["Завершение программы"]
id1[/Some text/]id1{{Some text}}


```id1[/Some text/]id1{{Some text}}id1{Some text}




![[тест.png]]
main
```mermaid
flowchart TD
    Start["Начало программы"] --> JumpBeg["Перейти на начало (jmp beg)"]
    JumpBeg --> DebugHandler["Сохранение старого обработчика прерывания"]
    DebugHandler --> SetNewHandler["Установка нового обработчика (TF флаг)"]
    SetNewHandler --> ThreadExec["Выполнение отлаживаемого кода (thread)"]
    ThreadExec --> ExitHandler["Восстановление старого обработчика"]
    ExitHandler --> WaitKey["Ожидание нажатия клавиши"]
    WaitKey --> ProgramEnd["Завершение программы"]
```


debug
```mermaid
flowchart TD
    DebugStart["Сохранение регистров, запрет прерываний"] --> PrintIP["Вывод IP"]
    PrintIP --> PrintAX["Вывод AX"]
    PrintAX --> WaitKeyDebug["Ожидание нажатия клавиши"]
    WaitKeyDebug --> RestoreRegs["Восстановление регистров, разрешение прерываний"]
    RestoreRegs --> ReturnFromDebug["IRET (возврат)"]
```
print_symbol
```mermaid
flowchart TD
    PushRegs["Сохранение регистров"] --> CheckZero["Проверка на 0"]
    CheckZero -->|Да| SkipPrint["Пропуск печати"]
    CheckZero -->|Нет| Int21h["Вызов прерывания для вывода символа"]
    SkipPrint --> RestoreSymbolRegs["Восстановление регистров"]
    Int21h --> RestoreSymbolRegs
    RestoreSymbolRegs --> SymbolReturn["Возврат"]
```

Print_number
```mermaid
flowchart TD
    SaveRegs["Сохранение регистров"] --> GetDigits["Выделение цифр числа"]
    GetDigits --> PushDigits["Сохранение цифр в стек"]
    PushDigits -->|AX = 0| PrintDigits["Вывод цифр"]
    PrintDigits --> RestoreNumberRegs["Восстановление регистров"]
    RestoreNumberRegs --> NumberReturn["Возврат"]
```
```mermaid
flowchart TD
    
        A[begin] --> B[writeln filename_m]
        B --> C[read_filename filename]
        C --> D[clearscreen]
        D --> E[open_file filename+2, handle, err]
        E -->|err = 1| F[writeln errOpen_m]
        E -->|err != 1| G[read_file handle 80, 10]
        F --> G
        G --> H{close_file handle}
        H --> I[end]

```
```mermaid
	flowchart TD
		macro1["writeChar MACRO chr<br>output character chr"]
		macro2["newline MACRO<br>output newline"]
		macro3["addEmptyLines MACRO<br>output 10 empty lines"]
		macro4["clearscreen MACRO<br>clear the console window"]
	macro5["write MACRO msg<br>output the string msg"]
	macro6["writeln MACRO msg<br>output the string msg<br>and make a newline"]
	macro7["read_filename MACRO filename<br>read a filename from input"]
	macro8["open_file MACRO filename, handle, err<br>open the file with full filepath filename,<br>get the file handle. If the file cannot be opened,<br>then make err = 1"]
	macro9["close_file MACRO handle<br>close the file handle"]
```
